import cat from 'img/cat.jpg'

# Heading 1 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

1. first
2. second
3. third

## Heading 2 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

- first
- second
- third

### Heading 3 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

#### Heading 4 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

##### Heading 5 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

###### Heading 6 (hello world this is voidjs for managing html pages)

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

This is a paragraph. (hello world this is voidjs for managing html pages). We have inline code `const framework = 'voidjs'` too.

# 一级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

1. 第一点
2. 第二点
3. 第三点

## 二级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

- 第一点
- 第二点
- 第三点

### 三级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

#### 四级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

##### 五级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

###### 六级标题 (你好，这里是 voidjs，用于管理静态页面)

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

这是正文部分。(你好，这里是 voidjs，用于管理静态页面)。此外还有个行内代码 `const framework = 'voidjs'`。

## Images

<figure>
  <img src={cat} alt="cat" />
  <figcaption>I'm a sleeping cat in figure tag.</figcaption>
</figure>

<div>
  <img src={cat} alt="cat" />
</div>

## Code

```js
var name = 'voidjs'
```